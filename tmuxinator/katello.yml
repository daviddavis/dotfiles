# ~/.tmuxinator/katello.yml
# you can make as many tabs as you wish...

project_name: katello
project_root: ~/foreman
pre_window:
windows:
  - keditor: cd ../katello && git fetch --all
  - feditor:
  - console: rc
  - server: rs
  - kshell: cd ../katello
  - foreman: git fetch --all
